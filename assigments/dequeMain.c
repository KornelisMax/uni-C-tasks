#include <stdio.h> 
#include <stdlib.h> 
#include "header.h"
#include "dequeFun.c"
 
/* Program to test DequeFunctions.c*/
int main() 
{ 
      int n;
  // *returnHead();
  // initializeDeque();
  isDequeFull();
  isDequeEmpty(&head);
  // int i = addFront(&head, 2); 
// printf("%d s",  i);
  // addRear(&head, 4); 
  // scanf("%d", &n);
  // addRear(&head, 2);
    // delFront(&head);
    // delFront(&head);
    // delFront(&head);
    // delRear(&head);
    // delRear(&head);
  // // delAll(&head);
  // delAll(&head);
  //   addfront(&head, 5);
  // printf("\n list is: "); 
  printDeque(head); 
  return 0; 
} 